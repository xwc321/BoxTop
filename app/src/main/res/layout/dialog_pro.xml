<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/pro_dialog_root"
    android:layout_width="400dp"
    android:layout_height="260dp"
    android:background="@drawable/dialog_bg_premium"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:padding="24dp">

    <!-- 图标 -->
    <ImageView
        android:id="@+id/pro_icon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:contentDescription="Pro Icon"
        android:src="@drawable/ic_pro"
        app:layout_constraintBottom_toTopOf="@id/pro_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.1" />

    <!-- 标题 -->
    <TextView
        android:id="@+id/pro_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="解锁 Pro 功能"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pro_icon" />

    <!-- 描述 -->
    <TextView
        android:id="@+id/pro_desc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="解锁后可享受高级动画、隐藏软件名、智能排序等功能"
        android:textColor="#CCCCCC"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pro_title" />

    <!-- 按钮 -->
    <Button
        android:id="@+id/pro_btn_unlock"
        android:layout_width="200dp"
        android:layout_height="48dp"
        android:layout_marginTop="24dp"
        android:background="@drawable/bg_pro_btn"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:text="立即解锁"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias=".1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pro_desc" />

    <Button
        android:id="@+id/pro_btn_unlock_close"
        android:layout_width="100dp"
        android:layout_height="30dp"
        android:background="@drawable/bg_pro_btn"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:text="放弃解锁"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/pro_btn_unlock" />

</androidx.constraintlayout.widget.ConstraintLayout>
